# Generative AI Track
# Shares the repository's virtual environment. Install the root requirements first:
#   pip install -r ../requirements.txt

# Module 01 — GenAI Landscape
transformers>=4.40.0
torch>=2.2.0
matplotlib>=3.8.0

# Later modules (added when each one is built)
# python-dotenv   -> API keys from .env
# faiss-cpu       -> vector search
# fastapi, uvicorn -> serving
