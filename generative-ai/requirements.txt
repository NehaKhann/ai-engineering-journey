# Generative AI Track
# Self-contained: this file lists everything the track needs, so you do NOT have to install the
# repository's root requirements first (those cover the weekly course and are much heavier).
#   pip install -r generative-ai/requirements.txt
# Modules 02 onward use the `dtype=` argument, which needs transformers 4.56 or newer.

# Modules 01 to 08 — core
transformers>=4.56.0
torch>=2.2.0
matplotlib>=3.8.0
scikit-learn>=1.3.0
chromadb>=0.5.0

# Module 03 and Module 07 — the anthropic package is only needed for the optional hosted-model sections
anthropic>=1.0.0

# Module 09 — fine-tuning experiment (LoRA)
peft>=0.11.0

# Module 10 — multimodal and diffusion
pillow>=10.0.0

# Module 11 and Project 04 — serving
fastapi>=0.110.0
uvicorn>=0.29.0
httpx>=0.27.0

# Project 02 — reading PDFs
pypdf>=4.0.0

# Optional: python-dotenv for loading API keys from a .env file
